server:
  port: 8080
spring:
  application:
    name: customer
  cloud:
    config:
      enabled: false

---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://config:8888
      failFast: true
      enabled: true
